{% extends 'back.html.twig' %}

{% block container %}
    <h1>Club creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-grd-success" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('club_index') }}" class="btn btn-inline">Back to the list</a>
        </li>
    </ul>
{% endblock %}
